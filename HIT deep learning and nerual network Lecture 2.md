# HIT deep learning and nerual network Lecture 2

## 全连接网络的问题

### 参数爆炸问题

对于全连接网络，当输入为图像时，参数量会急剧膨胀。例如：
- 输入为 $1000 \times 1000$ 的灰度图像时，输入层就有 $1\text{M} = 10^6$ 个神经元
- 若第一个隐藏层有 $1000$ 个神经元，则仅第一层的权重矩阵参数量就达到 $10^6 \times 1000 = 1 \times 10^{12}$
- 如此庞大的参数量导致训练困难、内存不足、过拟合严重

### 全连接网络的局限性

- 参数过多，训练效率低
- 忽略了图像的局部空间结构——图像中相邻像素之间的关联性是局部的
- 图像中的特征（如边缘、纹理、角点）具有**平移不变性**：某个特征在图像不同位置出现时，其语义意义是相同的

## 卷积神经网络的基本思想

### 生物学启发

1981年，Hubel 和 Wiesel 在研究猫的视觉皮层时发现：
- 视觉皮层中的神经元只对视野中特定局部区域（感受野）的刺激做出响应
- 不同神经元对不同方向、不同位置的边缘特征敏感
- 这种局部感受野机制启发了卷积神经网络的设计

### 三大核心思想

卷积神经网络通过以下三个机制解决全连接网络的问题：

1. **局部感受野（Local Receptive Field）**：每个神经元只连接输入的一小片局部区域，而不是全连接
2. **权值共享（Weight Sharing）**：在图像不同位置使用相同的卷积核（滤波器）提取相同特征，大幅减少参数
3. **下采样/池化（Subsampling / Pooling）**：通过池化操作逐步降低特征图分辨率，聚合统计信息

> 这三个机制分别带来了：参数量的显著下降、平移等变性、以及一定程度的平移不变性。

## 卷积神经网络的基本组成

### 卷积层（Convolution Layer）

#### 卷积运算

对于输入图像（或特征图）$\mathbf{X}$ 和卷积核 $\mathbf{W}$，二维卷积运算定义为：

$$
y_{i,j} = \sum_{m=0}^{k_h-1} \sum_{n=0}^{k_w-1} w_{m,n} \cdot x_{i+m,\; j+n}
$$

其中：
- $w_{m,n}$ 为卷积核在位置 $(m, n)$ 的权值
- $k_h, k_w$ 分别为卷积核的高度和宽度
- $y_{i,j}$ 为输出特征图在位置 $(i, j)$ 的值

> 卷积核在输入上滑动，每个位置计算内积，得到的输出称为**特征图（Feature Map）**。

#### 多通道卷积

对于多通道输入（如 RGB 图像有 3 个通道），卷积核也需要对应的通道数。第 $l$ 层第 $k$ 个输出通道的计算：

$$
y_{k,i,j} = \sum_{c=0}^{C_{in}-1} \sum_{m=0}^{k_h-1} \sum_{n=0}^{k_w-1} w_{k,c,m,n} \cdot x_{c,i+m,\; j+n} + b_k
$$

其中：
- $C_{in}$ 为输入通道数
- $w_{k,c,m,n}$ 为第 $k$ 个卷积核中对应第 $c$ 个输入通道的权值
- $b_k$ 为偏置项

### 卷积核超参数

#### 卷积核大小（Kernel Size）
常见的卷积核大小：$1 \times 1$、$3 \times 3$、$5 \times 5$、$7 \times 7$、$11 \times 11$。小卷积核（如 $3 \times 3$）堆叠可以获得与大卷积核相同的感受野，但参数更少、非线性更强。

#### 填充（Padding）
在输入特征图边缘补零（或其他填充方式），使得输出尺寸可控。常见的 padding 方式：
- **Valid / No Padding**：不做填充，输出尺寸会缩小
- **Same Padding**：填充使得输出尺寸与输入相同（stride = 1 时）

> 对于 $k \times k$ 的卷积核，same padding 通常在每边填充 $\lfloor k/2 \rfloor$ 个像素。

#### 步长（Stride）
控制卷积核在输入上滑动的步幅。设步长为 $s$，则输出特征图的尺寸为：

$$
\text{output\_size} = \left\lfloor \frac{\text{input\_size} - k + 2p}{s} \right\rfloor + 1
$$

其中 $k$ 为卷积核大小，$p$ 为 padding 大小。

> stride = 2 时，输出尺寸约为输入的一半，起到了下采样的效果。

#### 输出通道数
每个卷积层包含多个卷积核，每个卷积核生成一个输出通道（一张特征图）。输出通道数是一个可设的超参数，通常在网络中逐层增加（如 6 → 16 → 120）。

### 计算量分析

对于一层卷积：
- 输入尺寸：$H_{in} \times W_{in} \times C_{in}$
- 卷积核：$K_h \times K_w \times C_{in} \times C_{out}$
- 输出尺寸：$H_{out} \times W_{out} \times C_{out}$
- 计算量（乘加运算）：$H_{out} \times W_{out} \times C_{out} \times K_h \times K_w \times C_{in}$
- 参数量：$K_h \times K_w \times C_{in} \times C_{out} + C_{out}$

相比全连接层，卷积层的参数量与输入图像尺寸无关（仅与卷积核大小和通道数有关），参数量大减。

### 池化层（Pooling Layer）

池化层用于降采样特征图，减小空间尺寸，增强平移不变性。

#### 最大池化（Max Pooling）
取池化窗口内的最大值：

$$
y_{i,j} = \max_{m=0}^{p_h-1} \max_{n=0}^{p_w-1} x_{i \cdot s + m,\; j \cdot s + n}
$$

#### 平均池化（Mean Pooling / Average Pooling）
取池化窗口内的平均值：

$$
y_{i,j} = \frac{1}{p_h \cdot p_w} \sum_{m=0}^{p_h-1} \sum_{n=0}^{p_w-1} x_{i \cdot s + m,\; j \cdot s + n}
$$

> 常见的池化设置：$2 \times 2$ 窗口，stride = 2，输出特征图尺寸减半。

池化层**没有可学习参数**，不参与梯度更新。

### 卷积神经网络的一般结构

输入 → 卷积层 → 激活函数 → 池化层 → 卷积层 → 激活函数 → 池化层 → ... → 全连接层 → 全连接层 → Softmax → 输出

早期 CNN（如 LeNet-5）在每个卷积/池化阶段后使用 Sigmoid 或 Tanh 激活，现代 CNN 普遍使用 ReLU。

## 卷积神经网络的反向传播

CNN 的反向传播（BP 算法）仍然遵循链式法则，但需要处理卷积运算和池化运算的特殊性。

### 符号约定

- $\mathbf{z}^{[l]}$：第 $l$ 层的线性输出（卷积结果）
- $\mathbf{a}^{[l]}$：第 $l$ 层经过激活函数后的输出
- $\mathbf{W}^{[l]}$：第 $l$ 层的卷积核
- $\delta^{[l]}$：第 $l$ 层的局部梯度（误差项），$\delta^{[l]} = \frac{\partial J}{\partial \mathbf{z}^{[l]}}$

### 卷积层的前向传播

第 $l$ 层卷积层的第 $k$ 个输出通道：

$$
z_{k,i,j}^{[l]} = \sum_{c=0}^{C_{in}-1} \sum_{m=0}^{k_h-1} \sum_{n=0}^{k_w-1} w_{k,c,m,n}^{[l]} \cdot a_{c,i+m,\;j+n}^{[l-1]} + b_k^{[l]}
$$

$$
a_{k,i,j}^{[l]} = f(z_{k,i,j}^{[l]})
$$

### 卷积层的反向传播 —— 计算 $\delta^{[l]}$

已知第 $l+1$ 层的误差 $\delta^{[l+1]}$，需要计算第 $l$ 层的误差 $\delta^{[l]}$。

由于卷积操作中一个输入位置会影响输出特征图中的多个位置，因此求导时需要把所有这些影响的梯度**求和**：

$$
\delta_{c,i,j}^{[l]} = \sum_{k=0}^{C_{out}-1} \sum_{m=0}^{k_h-1} \sum_{n=0}^{k_w-1} \delta_{k,\; i-m,\; j-n}^{[l+1]} \cdot w_{k,c,m,n}^{[l+1]} \cdot f'(z_{c,i,j}^{[l]})
$$

> 这实际上是将 $\delta^{[l+1]}$ 与**翻转后的卷积核**做卷积（即转置卷积），再将结果与激活函数导数逐元素相乘。

简记为：

$$
\delta^{[l]} = \delta^{[l+1]} \circledast \text{rot180}(\mathbf{W}^{[l+1]}) \odot f'(\mathbf{z}^{[l]})
$$

其中 $\circledast$ 表示卷积操作，$\text{rot180}$ 表示将卷积核旋转 $180^\circ$（即上下左右翻转），$\odot$ 表示逐元素乘法。

### 卷积层的反向传播 —— 计算权值梯度

误差 $J$ 对卷积核权值 $w_{k,c,m,n}^{[l]}$ 的偏导：

$$
\frac{\partial J}{\partial w_{k,c,m,n}^{[l]}} = \sum_{i} \sum_{j} \delta_{k,i,j}^{[l]} \cdot a_{c,\; i+m,\; j+n}^{[l-1]}
$$

> 这相当于将误差 $\delta^{[l]}$ 与输入 $\mathbf{a}^{[l-1]}$ 做卷积。

偏置的梯度：

$$
\frac{\partial J}{\partial b_k^{[l]}} = \sum_{i} \sum_{j} \delta_{k,i,j}^{[l]}
$$

### 池化层的反向传播

池化层没有激活函数，所以只需要将梯度传递到上一层即可。

#### 最大池化的反向传播

对于最大池化，梯度只流向池化窗口中最大值所在的位置，其余位置梯度为零：

$$
\delta_{c,i,j}^{[l]} = \text{upsample}_{\text{max}}\left(\delta_{c,\; \lfloor i/s \rfloor,\; \lfloor j/s \rfloor}^{[l+1]}\right)
$$

即：记录前向传播时最大值的位置，反向传播时只将梯度"还原"到那些位置上。

> 对于 $2 \times 2$ 的最大池化，上采样时将每个梯度值放到 $2 \times 2$ 区域中前向最大值的位置，其他位置填 0。

#### 平均池化的反向传播

对于平均池化，梯度平均分配到池化窗口中的所有位置：

$$
\delta_{c,i,j}^{[l]} = \frac{1}{p_h \cdot p_w} \cdot \delta_{c,\; \lfloor i/s \rfloor,\; \lfloor j/s \rfloor}^{[l+1]}
$$

## 经典卷积神经网络 —— LeNet-5

LeNet-5 是 Yann LeCun 于 1998 年提出的用于手写数字识别（MNIST）的卷积神经网络，奠定了现代 CNN 的基本架构。

### 网络结构

```
输入 → C1(卷积) → S2(池化) → C3(卷积) → S4(池化) → C5(卷积/全连接) → F6(全连接) → Output(Softmax/RBF)
```

### 各层详解

#### 输入层
- 输入尺寸：$32 \times 32$ 的灰度图像（单通道）

#### C1 层 —— 第一个卷积层
- 卷积核大小：$5 \times 5$
- 输出通道数：**6 个特征图**
- 输出尺寸：$28 \times 28$（valid convolution，无 padding，$(32-5+1)=28$）
- 可训练参数：$(5 \times 5 + 1) \times 6 = 156$
- 连接数：$(5 \times 5 + 1) \times 6 \times (28 \times 28) = 122,304$
- 激活函数：Sigmoid / Tanh

#### S2 层 —— 第一个池化层
- 池化窗口：$2 \times 2$，步长 $2$
- 输出尺寸：$14 \times 14 \times 6$（尺寸减半）
- 池化方式：平均池化（Average Pooling）
- 可训练参数：0

#### C3 层 —— 第二个卷积层
- 卷积核大小：$5 \times 5$
- 输出通道数：**16 个特征图**
- 输入通道数：6（来自 S2 层的 6 个特征图）
- 输出尺寸：$10 \times 10$（$(14-5+1)=10$）
- LeNet-5 中 C3 层使用**非全连接**：16 个输出通道并不与所有 6 个输入通道全连接，而是按照设计好的连接表进行部分连接，以减少计算量、打破对称性
- 可训练参数（全连接假设）：$(5 \times 5 \times 6 + 1) \times 16 = 2,416$
- 激活函数：Sigmoid / Tanh

#### S4 层 —— 第二个池化层
- 池化窗口：$2 \times 2$，步长 $2$
- 输出尺寸：$5 \times 5 \times 16$
- 池化方式：平均池化
- 可训练参数：0

#### C5 层 —— 卷积/全连接层
- 卷积核大小：$5 \times 5$（与 S4 输出特征图尺寸相同，等价于全连接）
- 输出通道数：**120**
- 输出尺寸：$1 \times 1 \times 120$（$(5-5+1)=1$，每个通道退化为一个标量）
- 可训练参数：$(5 \times 5 \times 16 + 1) \times 120 = 48,120$
- 激活函数：Sigmoid / Tanh

#### F6 层 —— 全连接层
- 输入：120 维向量
- 输出：**84** 维向量
- 可训练参数：$(120 + 1) \times 84 = 10,164$
- 激活函数：Sigmoid / Tanh

#### 输出层
- 输入：84 维向量
- 输出：10 维（对应数字 0-9）
- LeNet-5 原始论文使用 **RBF（径向基函数）** 作为输出层，后来通常替换为 **Softmax**
- 损失函数：交叉熵损失

### LeNet-5 特点总结

| 特点 | 说明 |
|------|------|
| 激活函数 | Sigmoid / Tanh（非 ReLU） |
| 池化方式 | 平均池化（非最大池化） |
| 卷积 padding | Valid（无填充），尺寸逐层缩小 |
| 通道数变化 | $1 \rightarrow 6 \rightarrow 16 \rightarrow 120$，逐步增加 |
| 空间尺寸变化 | $32 \rightarrow 28 \rightarrow 14 \rightarrow 10 \rightarrow 5 \rightarrow 1$，逐步减小 |
| 非线性层 | 接在每次卷积后（LeNet-5 中 Sigmoid 放在池化后） |

## 经典图像分类网络

### AlexNet

AlexNet 由 Alex Krizhevsky 等人于 2012 年提出，在 ILSVRC-2012 竞赛中以巨大优势夺冠（top-5 错误率 15.3%，第二名 26.2%），标志着深度学习在计算机视觉领域的突破。

#### 网络结构
- **8 层结构**：5 个卷积层 + 3 个全连接层
- 输入尺寸：$224 \times 224 \times 3$（RGB 三通道）

#### AlexNet 的关键贡献

**1. ReLU 激活函数**
- 首次在 CNN 中使用 ReLU 非线性激活函数
- 相比 Sigmoid/Tanh，ReLU 缓解了梯度消失问题，训练速度快数倍

$$
\text{ReLU}(x) = \max(0, x)
$$

**2. Dropout 正则化**
- 在全连接层中引入 Dropout，训练时随机丢弃一部分神经元（置零）
- 有效防止过拟合

**3. 数据增强**
- 图像裁剪：从 $256 \times 256$ 的图像中随机裁剪 $224 \times 224$ 区域
- 水平翻转（flip）
- 颜色抖动（color jittering）：对 RGB 通道做 PCA 颜色扰动
- 有效增大训练集，提升泛化能力

**4. LRN（Local Response Normalization）**
- 局部响应归一化，模拟生物神经元的侧抑制机制
- 后被证明效果有限，现代网络中多被 Batch Normalization 取代

**5. 重叠池化（Overlapping Pooling）**
- 池化窗口 $3 \times 3$，步长 $2$
- 相邻池化窗口之间有重叠，提升特征的平移不变性

**6. 多 GPU 训练**
- 使用两块 GTX 580 GPU（各 3GB 显存）并行训练
- 网络被拆分到两个 GPU 上，仅在某些层进行通信

#### AlexNet 参数规模
- 总参数量约 **6000 万**
- ILSVRC-2010 测试结果：top-1 错误率 37.5%，top-5 错误率 17.0%

#### AlexNet 层详解（简化版）

| 层 | 类型 | 卷积核/池化 | 步长 | 输出尺寸 | 通道数 |
|---|---|---|---|---|---|
| conv1 | 卷积 | $11 \times 11$ | 4 | $55 \times 55$ | 96 |
| pool1 | 最大池化 | $3 \times 3$ | 2 | $27 \times 27$ | 96 |
| conv2 | 卷积 | $5 \times 5$ | 1 | $27 \times 27$ | 256 |
| pool2 | 最大池化 | $3 \times 3$ | 2 | $13 \times 13$ | 256 |
| conv3 | 卷积 | $3 \times 3$ | 1 | $13 \times 13$ | 384 |
| conv4 | 卷积 | $3 \times 3$ | 1 | $13 \times 13$ | 384 |
| conv5 | 卷积 | $3 \times 3$ | 1 | $13 \times 13$ | 256 |
| pool5 | 最大池化 | $3 \times 3$ | 2 | $6 \times 6$ | 256 |
| fc6 | 全连接 | — | — | 4096 | — |
| fc7 | 全连接 | — | — | 4096 | — |
| fc8 | 全连接 | — | — | 1000 | — |

### VGG-16

VGG 由牛津大学 VGG（Visual Geometry Group）于 2014 年提出，核心思想是**用深层的小卷积核替代浅层的大卷积核**。

#### 核心设计原则
- 所有卷积层统一使用 **$3 \times 3$** 卷积核，步长 $1$，**same padding** 保持尺寸不变
- 所有池化层统一使用 **$2 \times 2$** 最大池化，步长 $2$，尺寸减半
- 通道数随空间尺寸减小而翻倍：$64 \rightarrow 128 \rightarrow 256 \rightarrow 512$

> 两个 $3 \times 3$ 卷积叠加的感受野等于一个 $5 \times 5$ 卷积，三个 $3 \times 3$ 卷积叠加的感受野等于一个 $7 \times 7$ 卷积。但堆叠小卷积核：
> 1. 参数更少：$3 \times (3\times3\times C\times C) = 27C^2$ vs $7\times7\times C\times C = 49C^2$
> 2. 非线性更强：每层卷积后都有 ReLU，更多层带来更强的表达能力

#### 网络结构
- **16 层结构**：13 个卷积层 + 3 个全连接层
- VGG-16 总参数量约 **1.38 亿**
- 输入尺寸：$224 \times 224 \times 3$

#### VGG-16 vs VGG-19
VGG-19 在相同结构上增加了 3 个卷积层（共 16 个卷积层），参数量约 1.44 亿。在深层网络技术（如 Batch Normalization）成熟前，19 层过深反而影响性能。

### GoogLeNet / Inception

GoogLeNet（Inception v1）由 Google 于 2014 年提出，与 VGG 同年参加 ILSVRC-2014 并夺冠。核心创新是 **Inception 模块**。

#### Inception 模块的设计思想

在同一层中并行使用多种大小的卷积核（$1 \times 1$、$3 \times 3$、$5 \times 5$）以及池化，将它们的输出在通道维度上**拼接**：

1. **$1 \times 1$ 卷积**：提取精细的逐点特征，同时可以用于降维（减少通道数）
2. **$3 \times 3$ 卷积**：提取中等范围的空间特征
3. **$5 \times 5$ 卷积**：提取大范围的空间特征
4. **$3 \times 3$ 最大池化**：提供池化路径的特征

> **$1 \times 1$ 卷积的降维作用**：在 $3 \times 3$、$5 \times 5$ 卷积前先用 $1 \times 1$ 卷积降低通道数，可以大幅减少计算量。例如：
> - 不使用 $1 \times 1$：$5 \times 5$ 卷积直接处理 256 通道 → 计算量 $5 \times 5 \times 256 \times C_{out}$
> - 使用 $1 \times 1$：先降为 64 通道 → $1 \times 1 + 5 \times 5$ → 总计算量大减

#### GoogLeNet 特点
- 22 层结构（含参数的层），但参数量仅约 500 万（远小于 AlexNet 的 6000 万和 VGG-16 的 1.38 亿）
- 使用**全局平均池化（Global Average Pooling）** 替代最后的大全连接层
- 在中间层添加辅助分类器（auxiliary classifiers）以缓解梯度消失

#### 计算量对比

| 网络 | 参数量 | FLOPs（浮点运算量） |
|------|--------|---------------------|
| AlexNet | ~6000 万 | — |
| VGG-19 | ~1.44 亿 | 约 19.6 GFLOPs |
| GoogLeNet | ~500 万 | 约 1.5 GFLOPs |
| ResNet-34 | ~2170 万 | 约 3.6 GFLOPs（仅为 VGG-19 的约 18%） |

## 卷积神经网络的设计趋势总结

1. **小卷积核堆叠替代大卷积核**：$3 \times 3$ 成为标准，多个小卷积核堆叠 = 相同感受野 + 更少参数 + 更强非线性
2. **更深更窄**：网络深度增加，每层通道数适度增长
3. **池化层逐步被步长卷积替代**：stride = 2 的卷积也能实现下采样
4. **全连接层逐步被全局平均池化替代**：减少参数，防止过拟合
5. **激活函数**：Sigmoid/Tanh → ReLU → ReLU 变体（LeakyReLU、PReLU、ELU 等）
6. **归一化技术**：LRN → Batch Normalization → Layer Normalization / Group Normalization
